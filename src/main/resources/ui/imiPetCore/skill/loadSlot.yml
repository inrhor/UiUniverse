loadSlot:
  title: "宠物 | 选择装载的槽位"
  # 继承模板
  template: page
  slots: [20, 22, 24]
  # 多页数据导入
  data: load-slot
  icon:
    item:
      data:
        - "MATERIAL:loadSlot"
        - "LORE:loadSlot"
      name: "&f                                                  "
    script: |
      petSkill load
      ui open skillManager namespace imipet
